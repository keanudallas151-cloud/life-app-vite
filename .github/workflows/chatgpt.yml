name: ChatGPT Integration

on:
  workflow_dispatch:

jobs:
  chatgpt:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: ChatGPT Integration
        uses: kxxt/chatgpt-action@v0.3
